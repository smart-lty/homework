# 机器学习课程第一次作业
+ P48，2.5t
+ 编程实现下列数据的ROC曲线

数据如图所示：图中有20个样本，`Inst#`是样本序号。`Class`是`ground truth` 标签，`p`是`positive`样本（正例）,`n`当然就是`negative`（负例）， `score`是分类器判断该样本属于正例的概率。



编程实现。要求源代码有注释并拷贝到答案中，并把源代码作为附件附上。

（1）根据这个数据，画出`PR`曲线，`ROC`曲线。

（2）代价矩阵`cost01`=5，`cost10`=2，请画出代价曲线。

请尽量不要***直接***使用第三方的库函数
